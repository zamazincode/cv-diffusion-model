# Core dependencies
torch>=2.0.0
torchvision>=0.15.0
diffusers>=0.21.0
transformers>=4.30.0
accelerate>=0.20.0
safetensors>=0.3.0
huggingface-hub>=0.16.4,<1.0

# Training
pytorch-lightning>=2.0.0
wandb>=0.15.0  # Optional: For experiment tracking
peft>=0.5.0  # Optional: LoRA desteği

# Image processing
opencv-python>=4.8.0,<4.12.0  # NumPy 1.x uyumluluğu için
albumentations>=1.3.0
pillow>=10.0.0
rawpy>=0.18.0  # RAW image support

# Quantization & Export
onnx>=1.12.0
onnxruntime>=1.12.0
# onnxruntime-gpu>=1.12.0  # Optional: Only if GPU inference needed
onnxsim>=0.4.0  # ONNX simplifier
# ai-edge-torch>=0.1.0  # Optional: TFLite export (experimental)
# neural-compressor>=2.0.0  # Optional: Intel quantization

# Metrics & Evaluation
torchmetrics>=1.2.0
lpips>=0.1.4
pytorch-fid>=0.3.0

# Utilities
numpy>=1.24.0,<2.0.0  # NumPy 2.0 wandb ile uyumsuz
tqdm>=4.66.0
pyyaml>=6.0.0
einops>=0.7.0
timm>=0.9.0

